<form method="post" action="{{ path('app_birthday_delete', {'id': birthday.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ birthday.id) }}">
    <button class="btn">Delete</button>
</form>
